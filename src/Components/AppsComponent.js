import React from 'react';
import CrudDashboard from '../crud/CrudDashboard';
const AppsComponent = () => {
    return (
        <div style={{ marginTop: '1rem' }}>
            <CrudDashboard />
        </div>
    );
};

export default AppsComponent;
